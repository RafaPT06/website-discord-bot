// OAuth will be wired here next.
// Keeping this file separate now prevents the main website logic from becoming messy later.
export function initAuth() {
  // Coming soon: Discord OAuth login state, session checks and dashboard link handling.
}
